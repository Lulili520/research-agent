# 项目结构与本轮调整

本分支只维护通用 Agent：`agent/` 随科研策略演进，`research/` 保存本地研究材料，课题专用实现及依赖在独立仓库或其他分支维护。不把历史课题需求上移成所有研究的硬门禁。

## 控制平面模块

```text
agent/runtime/research/
  policy.py          schema/gate-policy 版本、文件布局、阶段与转换表
  storage.py         JSON/JSONL、项目锁、事件链和项目路径定位
  stdio.py           Windows 安全的确定性 UTF-8 命令行输出
  execution.py       冻结文件集合、版本授权、run 产物核验和实证状态推导
  gates.py           科学材料的结构检查及统一阶段门禁
  researchctl.py     命令解析、状态变更和事件登记
  audit.py           只读审计入口，共用 gates.py
  collect_openalex.py 调用方配置驱动的文献发现工具
```

依赖从 policy、storage 指向 execution，再指向 gates 和 CLI；底层模块不导入 CLI，也不导入任何课题模块。为兼容已有调用方，`researchctl.py` 暂时重导出原有函数；新代码优先从其所属模块导入。

此次拆分针对已发现的故障边界：审计与状态转换不再分别维护阶段阈值，协议冻结和执行验证不再散落在命令实现中，实证进度统一从运行证据计算。没有为每种课题引入插件框架或独立服务，避免增加尚无实际调用需求的层级。

## 已固化的不变量

- 每个 Research 进程通过 `RESEARCH_PROJECT_ROOT` 绑定唯一活动项目；控制、审计和采集入口在读取元数据前拒绝未绑定及跨项目请求，项目内路径解析不得越过 `.research/`。
- Agent 在活动研究上下文中不得枚举或读取兄弟项目。OpenAlex 的查询和输出都限定在当前项目 `review/`，并在写入时使用项目锁。
- 设计、分析计划、理论预测等冻结成员必须同时校验当前文件、归档、协议锁及事件。
- 执行指令证据与具体协议版本绑定；资源权限来自冻结设计中的显式声明。
- 主实验不能只靠 Markdown 中的 `pass` 进入，需要当前协议的可核验 Pilot 运行。
- 阶段变更不等于实验完成；机器实证状态来自登记终局、阶段、配置与结果哈希。
- 外部、生成和无数据研究分别描述材料来源，不强迫固定数据集数量或课题专用章节。
- 门禁策略升级必须显式迁移，并保存旧材料；旧记录不被补写成已满足新版要求。

## 测试分层

`agent/tests/test_researchctl.py` 保留原控制平面回归；`agent/tests/test_topic_independence.py` 检查算法、系统与理论问题使用相同协议入口；`agent/tests/test_history_isolation.py` 验证活动项目绑定、跨项目拒绝、不泄露和不改写兄弟历史；`agent/tests/test_execution.py` 验证绕过路径和真实的状态转换。测试全部使用临时项目和模拟产物，不执行研究实验。课题专用测试随课题实现维护，不再纳入本分支。默认测试通过只证明这些实现性质，不证明研究内容正确。

## 后续优化顺序

1. 将历史大型测试中的材料构造器提取为独立 fixture 模块，再按协议、授权、状态迁移和证据分组测试；迁移前后保持相同行为断言。
2. 课题源码、数据适配器和执行环境在独立项目中演进；本仓库只维护输入与证据契约。
3. 有第二个真实执行器后，再提炼通用执行适配接口。当前控制平面只登记和验收，不自动调度 GPU、SSH 或外部发布。

历史范围绑定和事件链都不是系统安全边界：前者防止正常工具和 Agent 上下文串用，后者发现非预期修改，但拥有工作区文件权限的人仍可直接读取文件或重算整条链。需要抵御这类访问时应使用独立账户、容器或工作区。授权证据内容与独立审查质量仍需要可信编排和人工监督；文件格式与哈希不能替代科学判断。

## 仓库入口

根目录仅保留 `AGENTS.md`、`README.md` 和 `agent/` 作为可见源码入口；实际课题创建后才出现 `research/`。测试、结构说明、开发依赖及 pytest 配置统一收拢到 `agent/`，使用 `python agent/check.py` 运行检查。`.github/` 保留在 GitHub 要求的位置，用于 CI；`.git/` 和 `.gitignore` 用于版本管理。
